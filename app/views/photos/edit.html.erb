<div class="photo-edit">
  <div class="container">
    <div class="img_container">
      <%= image_tag @photo.image %>
    </div>

    <%= form_for(@photo) do |f| %>
      <%= f.text_area :caption %>
      <%= f.submit "Update", :class => "form-btn" %>
    <% end %>

  </div>
</div>
